@{
    var currentPath = Context.Request.Path.ToString().ToLower();
    if (currentPath.StartsWith("/admin"))
    {
        Layout = "~/Views/Shared/_AdminLayout.cshtml";  // Your custom layout for /Auth
    }
    else
    {
        Layout = "~/Views/Shared/_Layout.cshtml";  // Default layout for other paths
    }
}